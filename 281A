#include<bits/stdc++.h>
using namespace std;

void solve(string str){
    if(str[0] >= 'A' && str[0] <= 'Z')
    {
        cout << str;
    }
    else{
        str[0] = toupper(str[0]);
        cout << str;
    }

}

int main()
{
    string str;
    cin >> str;
    solve(str);
    return 0;
}
